

#include "cprocessing.h"

/// Initializes the main menu state.
void InitializeMainMenu(void);

/// Updates the main menu state. Handles button rendering and click detection.
void UpdateMainMenu(void);

/// Cleans up resources used by the main menu (e.g., freeing images).
void ExitMainMenu(void);

/// Draws the title screen with buttons for navigation.
void UpdateTitleScreen(void);

